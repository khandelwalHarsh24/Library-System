server:
  port: 3000
spring:
  application:
    name: BorrowServiceMS
  datasource:
    url: jdbc:mysql://localhost:3306/borrowdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true