server:
  port: 4000
spring:
  application:
    name: BookServiceMS
  datasource:
    url: jdbc:mysql://localhost:3306/bookdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true